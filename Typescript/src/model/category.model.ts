export class Category {
    constructor(public title: string) {
    // construindo método construtor para a classe Category, onde os parâmetros title, price e imagemeUrl são definidos como propriedades públicas da classe. O parâmetro imagemeUrl é opcional, indicado pelo símbolo de interrogação (?), o que significa que ele pode ser omitido ao criar uma instância da classe Category.
    }
}